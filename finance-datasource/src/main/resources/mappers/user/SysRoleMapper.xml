<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.com.mz.app.finance.datasource.mysql.mapper.user.SysRoleMapper">

    <select id="selectRolesByUserId" resultType="cn.com.mz.app.finance.datasource.mysql.entity.permission.SysRoleDO">
        SELECT r.role_id, r.role_name, r.role_key, r.role_sort, r.data_scope,
               r.menu_check_strictly, r.dept_check_strictly, r.status, r.del_flag,
               r.create_by, r.create_time, r.update_by, r.update_time, r.remark
        FROM sys_role r
        LEFT JOIN sys_user_role ur ON ur.role_id = r.role_id
        WHERE ur.user_id = #{userId} AND r.del_flag = '0' AND r.status = '0'
    </select>

    <select id="selectRolePermissionByUserId" resultType="java.lang.String">
        SELECT DISTINCT r.role_key
        FROM sys_role r
        LEFT JOIN sys_user_role ur ON ur.role_id = r.role_id
        WHERE ur.user_id = #{userId} AND r.del_flag = '0' AND r.status = '0'
    </select>

    <select id="checkRoleNameUnique" resultType="cn.com.mz.app.finance.datasource.mysql.entity.permission.SysRoleDO">
        SELECT role_id, role_name, role_key, role_sort, data_scope,
               menu_check_strictly, dept_check_strictly, status, del_flag,
               create_by, create_time, update_by, update_time, remark
        FROM sys_role
        WHERE role_name = #{roleName} AND del_flag = '0'
        LIMIT 1
    </select>

    <select id="checkRoleKeyUnique" resultType="cn.com.mz.app.finance.datasource.mysql.entity.permission.SysRoleDO">
        SELECT role_id, role_name, role_key, role_sort, data_scope,
               menu_check_strictly, dept_check_strictly, status, del_flag,
               create_by, create_time, update_by, update_time, remark
        FROM sys_role
        WHERE role_key = #{roleKey} AND del_flag = '0'
        LIMIT 1
    </select>

</mapper>
